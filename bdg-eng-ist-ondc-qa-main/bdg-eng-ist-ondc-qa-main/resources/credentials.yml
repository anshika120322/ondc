# Add any required credential here. eg:  
elastic: # key of the credential
  cluster_id: null # Elastic cluster id encrypted
  user: null # ELK user
  password: null # ELK password encrypted

teams: # MS Teams webhook url
  hook_url: null # Teams webhook url encrypted

aws: # AWS credentials
  region: us-east-2
  access_key: null # AWS access key encrypted
  secret_key: null # AWS secret key encrypted